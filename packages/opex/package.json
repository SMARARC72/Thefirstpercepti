{
  "name": "@first-perception/opex",
  "version": "0.1.0",
  "description": "Opex throttle layer — daily-cap counter, per-session token budget, opex_event ledger on Vercel Postgres",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "@first-perception/types": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
