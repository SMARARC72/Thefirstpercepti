{
  "name": "@first-perception/opex",
  "version": "0.2.0",
  "description": "Opex throttle layer — daily-cap counter, per-session token budget, opex_event ledger on Supabase Postgres (via postgres SDK pointed at POSTGRES_URL)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "postgres": "^3.4.0",
    "@first-perception/types": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
